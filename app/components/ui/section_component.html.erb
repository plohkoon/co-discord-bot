<section class="<%= @classes %>">
  <h2 class="text-xs font-medium text-muted-foreground uppercase tracking-wide mb-3">
    <%= @title %><% if @meta %> <span class="text-muted-foreground/60"><%= @meta %></span><% end %>
  </h2>
  <%= content %>
</section>
